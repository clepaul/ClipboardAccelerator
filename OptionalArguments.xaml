<Window x:Class="ClipboardAccelerator.OptionalArguments"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClipboardAccelerator"
        mc:Ignorable="d"
        Title="Optional Arguments" Height="200" Width="500" ShowInTaskbar="False" MinWidth="300" MinHeight="200" ScrollViewer.HorizontalScrollBarVisibility="Visible" SizeToContent="Height" WindowStartupLocation="CenterOwner">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel x:Name="oaStackPanel" Margin="5">
        
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Margin" Value="5,10,5,0"/>
                </Style>
            </StackPanel.Resources>
           
        </StackPanel>
    </ScrollViewer>

</Window>
